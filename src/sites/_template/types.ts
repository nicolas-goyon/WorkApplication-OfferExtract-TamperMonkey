/**
 * TEMPLATE — copy this folder to src/sites/<site-name>/ when adding a new
 * site. Extend OfferData (from ../generic/types) only if the site can
 * expose fields the generic shape doesn't cover yet.
 */
export type { OfferData } from '../generic/types';
